%% Define root paths

Defaults
addpath(phToolsPath)

%% Define job parameters, include Region, supercatchment

groupArea = 'Ache';
supercatchmentNum = [1:7];
outputFigType = 'png';
peakElevationForOutputFig = 4500;
%%
RunPH
